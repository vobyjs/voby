{
  "name": "voby",
  "description": "A wannabe high-performance framework for writing web apps.",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc --skipLibCheck",
    "compile:watch": "tsc --skipLibCheck --watch",
    "bench": "rm -rf node_modules/.vite && vite --config vite.ts benchmark --force",
    "play": "rm -rf node_modules/.vite && vite --config vite.ts playground --force",
    "prepublishOnly": "npm run clean && npm run compile && npm run test"
  },
  "bugs": {
    "url": "https://github.com/fabiospampinato/voby/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Fabio Spampinato",
    "email": "spampinabio@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fabiospampinato/voby.git"
  },
  "keywords": [
    "ui",
    "framework",
    "reactive",
    "observable",
    "fast",
    "performant",
    "small",
    "fine-grained"
  ],
  "dependencies": {
    "oby": "^2.1.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5",
    "vite": "^2.7.13"
  }
}
