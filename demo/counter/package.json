{
  "private": true,
  "name": "counter",
  "scripts": {
    "dev": "DEV=1 vite --force --config vite.ts",
    "prod": "PROD=1 vite --force --config vite.ts"
  },
  "dependencies": {
    "vite": "^2.8.4",
    "voby": "*"
  }
}
